{
	"private": true,
	"type": "module",
	"scripts": {
		"tw:watch": "npx tailwindcss -c ./tailwind.config.js -i ./src/styles/tailwind.css -o ./src/styles/computed.css --watch",
		"dev": "vite",
		"build": "vite build",
		"preview": "vite preview",
		"git:deploy": "git subtree push --prefix dist origin main"
	},
	"dependencies": {
		"preact": "^10.13.1",
		"tailwindcss": "^3.4.1"
	},
	"devDependencies": {
		"@preact/preset-vite": "^2.5.0",
		"eslint": "^8.56.0",
		"eslint-config-preact": "^1.3.0",
		"vite": "^4.3.2"
	},
	"eslintConfig": {
		"extends": "preact"
	}
}
